import { Component } from '@angular/core';

@Component({
    standalone: true,
    selector: 'app-lazy-routing-sample',
    template: `HELLO WORLD FROM LAZY MODULE`,
})
export class LazyRoutingSampleComponent {}
