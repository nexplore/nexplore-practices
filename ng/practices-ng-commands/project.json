{
    "name": "practices-ng-commands",
    "$schema": "../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "practices-ng-commands/src",
    "prefix": "pui",
    "projectType": "library",
    "tags": [],
    "targets": {
        "build": {
            "executor": "@nx/angular:package",
            "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
            "options": {
                "project": "practices-ng-commands/ng-package.json"
            },
            "configurations": {
                "production": {
                    "tsConfig": "practices-ng-commands/tsconfig.lib.prod.json"
                },
                "development": {
                    "tsConfig": "practices-ng-commands/tsconfig.lib.json"
                }
            },
            "defaultConfiguration": "production"
        },
        "test": {
            "executor": "@nx/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "practices-ng-commands/jest.config.ts"
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint"
        }
    }
}
